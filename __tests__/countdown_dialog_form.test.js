import CountdownForm from '../components/CountdownForm';
import React from 'react';
import {mount} from 'enzyme';

describe('Test dialog form', () => {
  it('displays the details of the countdown that is being edited', () => {
    
  });
})
